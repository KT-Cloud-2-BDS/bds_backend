spring:
  application:
    name: order-test

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

logging:
  level:
    com.bds: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.modulith: DEBUG