spring:
  datasource:
    url: jdbc:h2:mem:authdb;MODE=MySQL
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create

  data:
    redis:
      host: localhost
      port: 6379
jwt:
  secret: "test-secret-key-must-be-long-enough-for-hs256-algorithm"
  expiration:
    access: 3600000
    refresh: 604800000

MAIL_USERNAME: "test-user@naver.com"
MAIL_PASSWORD: "test-password"