name: CI - gateway-service

# gateway-service 디렉토리 변경 시에만 실행
on:
  push:
    branches: [main, develop]
    paths:
      - 'services/gateway-service/**'
  pull_request:
    branches: [develop]
    paths:
      - 'services/gateway-service/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            services/gateway-service
            modules/common
            build.gradle
            settings.gradle
            gradlew
            gradle

      - uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Build and Test
        run: ./gradlew :services:gateway-service:clean :services:gateway-service:build :services:gateway-service:jacocoTestReport --stacktrace

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: gateway-service
          path: services/gateway-service/build/libs/*[!plain].jar

      - name: Upload Gateway Service Coverage Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-gateway-report
          path: services/gateway-service/build/reports/jacoco/test/html/
